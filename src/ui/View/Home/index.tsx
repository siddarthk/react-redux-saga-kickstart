import React from 'react'
import { PageWrapper } from '../../Layout';

interface Props {
  
}

const Home = (props: Props) => {
  const { } = props;
  
  return (
    <PageWrapper>
      Home Pagesss
    </PageWrapper>
  )
}

export default Home
